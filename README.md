# Mxspub

`Mxspub` is an Obsidian plugin for publishing Markdown notes to mx-space.

It publishes the active Markdown file as a post, note, or page, keeps publish
metadata in frontmatter, and uses Obsidian's `requestUrl`/`SecretStorage` APIs
for API calls and credentials.

## Features

- Publish the current file to mx-space as a post, note, or page.
- Update existing remote content when `mxspace.id` is present.
- Unpublish posts and notes by switching the remote entry back to draft.
- Resolve or create missing post categories and note topics.
- Send Markdown as the source body while also generating a Lexical JSON
  snapshot for mx-core editors.
- Store API keys through Obsidian SecretStorage.
- Check whether the selected API key can authenticate before publishing.

## Frontmatter

Common publish metadata lives under `mxspace`:

```yaml
mxspace:
  type: post
  id: "1234567890"
  slug: "example"
  state: draft
  lastPublishedAt: "2026-05-18T12:00:00.000Z"
```

Type-specific fields stay at the top level:

```yaml
title: "Example"
created: "2026-05-01T09:00:00.000Z"
category: "tech"
tags: ["obsidian", "mx-space"]
summary: "Summary"
mxspace:
  type: post
  state: draft
```

Supported `mxspace.type` values are `post`, `note`, and `page`. Supported
`mxspace.state` values are `draft` and `publish`; state only applies to posts
and notes.

## Development

Install dependencies:

```sh
pnpm install
```

Build the plugin:

```sh
pnpm build
```

Run TypeScript only:

```sh
pnpm typecheck
```

During development, `main.js` is generated by the build and is intentionally
ignored by Git.

## Documentation

See [docs/implementation.md](docs/implementation.md) for the implementation
contract, API behavior, and acceptance checklist.
