{
  "name": "mxspub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs production && pnpm run check:mobile",
    "check:mobile": "node scripts/check-mobile-safe.mjs",
    "dev": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "obsidian": "^1.11.4",
    "typescript": "^6.0.3"
  }
}
